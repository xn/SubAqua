# SDD ledger — plan: docs/superpowers/plans/2026-08-12-subaqua-sorceress.md

Worktree: /Users/xn/sites/KOL/SubAqua/.claude/worktrees/phase4-sorceress (branch worktree-phase4-sorceress, reset --hard to local main 9aa261b on 2026-08-27)
Spec: docs/superpowers/specs/2026-08-11-subaqua-design.md (read §5, §6, §9)
Verification cycle (plan Global Constraints): `yarn check && yarn lint` every task; `yarn build` additionally at Tasks 3, 4, 7, 12, 14. No unit test suite exists in this repo (`yarn test` is a stub).

## Baseline (2026-08-27)

- `yarn check` clean on main 9aa261b.
- `yarn lint` FAILS on main 9aa261b: 9 libram/verify-constants errors in src/resources/fishy.ts (merged via PR #1 a0d618d): `$item` for beefy/glistening/slick nigiri (pseudo-items — no items.txt row), `super-sweet fish goo` (real name `super-sweet fish goo (spoiled)`), `Sea jelly`/`Fish sauce` (real names lowercase).
- Ruling: fix the baseline in a "Task 0" commit on this branch before Task 1 — plan Global Constraints make `yarn lint` the per-task gate and it must start green; the nigiri `$item`s are a real defect (Item.none at runtime), not plugin lag — why: every task's verification would otherwise be ambiguous — cost if wrong: a small out-of-scope diff on the branch that is easy to drop.

## Pre-flight scan

(pending — preflight-scan.md written by scan agent)

## Tasks

Task 0: implemented e1a903f (fishy.ts lint baseline); review (sonnet): spec ✅, Approved, 1 Important — nigiri cost proxy understates true cost ~2× (fish-meat fullness 1 vs nigiri 2; toInt on "2-3" ranges is 0 so fullness dominates) — entering fix round 1.
Task 0: minor (deferred): pre-existing `fishyOpportunityCost()` `(7 - toInt(adventures))` term is a no-op for ranged-adventure items (mafia to_int("2-3") → 0) — affects every rung equally; not introduced by Task 0.
Task 0: fix round 1/5 (1 addressed, 0 open — nigiri fullness override; commits e1a903f..f57b8b6)
Task 0: complete (commits 9aa261b..f57b8b6, review clean)

## Pre-flight scan rulings (scan: preflight-scan.md — 51 cross-task rows, 14 per-task rows, 24 findings; all names checked against mafia data except F5/F22 casing)

- F1 Ruling: `Rng` is not exported by kolmafia typings — Task 1 types the handle structurally `type Rng = ReturnType<typeof phpSeed>` — cost if wrong: compile error, instant.
- F2 Ruling: empty candidate list must not poison the cache — on `seeds.length === 0` Task 1 writes `subaqua_seedScanFloor = constraintCount(c)` (and a recognizable empty-cache state) so the scan is never repeated for the same-or-fewer constraints; zero candidates is monotone under added clues — cost if wrong: a 9M-seed rescan on every post() (minutes per task).
- F3 Ruling: `candidateSeeds()`/`isKnucklebonesAndSushiEnough()` are memoized at module level keyed on (turnsPlayed, dreadScroll1..8, seedCandidates pref) and skip `set()` when unchanged — grimoire evaluates ready()/completed() for every task each pass — cost if wrong: per-pass 2000×8 MT draws, slow but correct.
- F4 Ruling: gym NC-forcer FIRING gates on the skate war, per Task 7 amendment (spec: honor CCS:1067-1070) — `gladiatorFilter(opts: { gym?: boolean; warOpen?: boolean } = {})`; `gymnasiumTurn()` passes its already-computed `skateWarOpen()`; filters never page-load — cost if wrong: wasted spike/avalanche casts.
- F5 Ruling: skill name is `McHugeLarge Avalanche` (classskills.txt:1120) in both the probe and the `$skill` template — cost if wrong: dead rung.
- F6 Ruling: Task 7 skatepark.ts adds `booleanModifier` to the kolmafia import and drops unused `have` — cost: none.
- F7 Ruling: Task 13 imports `unequip` from libram (not kolmafia) and drops unused `availableAmount` — cost: none.
- F8 Ruling: Task 8 deletes the `return [3, 6]` after `abort()` (abort is typed `never`; allowUnreachableCode:false) — cost: none.
- F9 Ruling: Task 8 amendment (clear Beaten Up only once ore is held) is honored structurally: the dig task's `do()` performs the whole available mining allotment before returning so engine `post()`'s global Beaten Up cleanup fires once, after mining; if the dig task cannot be structured that way, add `src/engine/engine.ts` to Task 8's files and carve mining out of the unconditional uneffect — cost if wrong: one Beaten Up cure per cave-in.
- F10 Ruling: choice.ts 703 fallbacks raise the unknowns guard to `> 5` (4^5 candidates is trivial) and perturb against `dreadScrollGuesses` so successive guesses differ — spec §6 "every handler branch answers something" AND must not loop on one wrong answer — cost if wrong: repeated identical wrong guess burning Deep-Tainted cycles.
- F11 Ruling: Task 7's lutz Fishy rung goes immediately after Rung 1 (fishy pipe) and BEFORE the cheapestFishySource optimizer (currently labelled "Rung 1.5" at fishy.ts:305); renumber (lutz = 1.5, optimizer = 1.6) — the deferral pricing at plan L61 requires the free rung to win — cost if wrong: a consumable spent while a free buff exists.
- F12 Ruling: Task 11 names the amendment's BCZ predicate separately (`bczWanted()`); `scrollsMissing` unchanged so the saber/monodent decision is not retuned — cost if wrong: BCZ equipped on the wrong turns.
- F13 Ruling: Task 12 executes in its AMENDED form (amendment supersedes the code block: YogHealingsNeeded/Owned model, trueHPPercent/YogHpCheck + antidote pull + uneffect Gummiheart, "exp" familiar); Task 12's Files list gains `src/tasks/sorceress/fights.ts` to amend `yogUrtFilter` (re-opening Task 5's file; step === 4 renumbers to 5). Task 5 builds `yogUrtFilter` exactly as its code block says and notes Task 12 will amend it — cost if wrong: fights.ts churn across two reviews.
- F14 Ruling: one exported helper `expFamiliar(): Familiar | undefined` (Chest Mimic → Cooler Yeti → Cookbookbat → undefined) lands in `src/engine/outfit.ts` beside `sneakFamiliar()` in Task 12; Task 13's Shub and Seaceress outfits call it — cost if wrong: none beyond a helper.
- F15 Ruling: Task 14's parasol/`haveAnywhere` amendment is a NO-PORT — no late-pull ladder exists in this plan; the only parasol site (pulls.ts:53,65-69) keeps `availableAmount` per the same amendment. Revisit if a smoke test shows a parasol pull while a GAP/navel ring sits in storage — cost if wrong: one wasted pull.
- F16 Ruling: Task 14 README gains one line under Usage: `subaqua sim` prints the permable-skill checklist (required / big turn saver / optional) — cost: none.
- F17 Ruling: naming contract amended: `gladiatorFilter(opts?: { gym?: boolean; warOpen?: boolean }): CombatFilter`; add `colosseumRoundTurn(): void` (Task 6 produces, Task 7 burn.ts consumes) — cost: none.
- F18 Ruling: STANDING RULE for every implementer — run `yarn lint:fix` (prettier import order is machine-enforced) before committing; `yarn check && yarn lint` must be green at every commit — cost: none.
- F19 Ruling: Tasks 6 and 7 execute as ONE dispatch unit (skatepark.ts is imported by Task 6's gym.ts); commits may stay per-task but tsc+lint must be green at the unit's final commit, and the unit is reviewed as one package — cost if wrong: a larger review surface.
- F20 Ruling: `skateWarOpen()` refreshes `sea_skatepark.php` at most once per `turnsPlayed()` value (module-level memo) and otherwise reads `skateParkStatus`; grimoire calls ready()/completed() for every task each pass — cost if wrong: one extra page load per turn.
- F21 Ruling: Task 6 rewrites the `gearQuest` comment to describe the `&&`; code stands — cost: none.
- F22 Ruling: item is `Monodent of the Sea` (items.txt:12004) — cost: lint error.
- F23 Ruling: Tasks 12/13 wrap the null-day exploit `pullSequence` in `if (pullBudgetAllows($item\`null-day exploit\`))` per pulls.ts:136-139 — cost if wrong: a reserved pull spent twice.
- F24 Ruling: `burnTurnElsewhere()` skips the gymnasium rung while `noncombatForcerActive` is true (falls through to colosseum / returns false) so the ladder never trips `gymnasiumTurn()`'s abort — cost if wrong: one burn turn spent in the colosseum instead of the gym.
- Spec check: plan's choice-1387/colosseum-saber invariant holds across all Phase 4 sites (scan §4 scope note).
  Task 1: implemented 624cc54 (DONE_WITH_CONCERNS: SF cite is seedfinder_util.ash:4 not seedfinder.ash:4 — cosmetic; seedScanFloor not ascension-scoped — handed to reviewer); review dispatched (opus).
  Task 2: implemented 84c3543 (DONE); review dispatched (sonnet).
  Task 1: review (opus): spec ✅ (RNG port verified vs seedfinder entry-by-entry), Needs fixes — 2 Important: seedScanFloor not ascension-scoped (can permanently disable the scan next ascension); floor conflates overflow-retry vs zero-result meanings. Ruling: parseInt/NaN filter minor promoted to the fix round (post() must never throw). Fix round 1 dispatched.
  Task 1: minor (deferred): dreadSeedCheck prints the red "zero candidates" line every post() once SCANNED_EMPTY latches (module-level once-flag would fix); matches() builds all 8 draws before comparing — early-exit on first known-clue mismatch would be 2-3x on the 9M loop, and the loop has no progress output; cache re-filter runs once redundantly after a full scan (memo key computed before pref write); comment cite ":233 SF:106-110" is SeedCriteria.ash:110-114; candidateSeeds() ignores dreadScrollGuesses (matches seedfinder; a wrong-count-0 guess pins all clues — a later task/final review should own that deliberately).
  Task 3: implemented f6d3e25 (DONE; choice.ts only); review dispatched (sonnet).
  Task 1: fix round 1/5 (3 addressed per implementer, pending re-review — floor ascension-scoped, single-owner overflow, parseInt radix+bounds; commit 183ff8c); re-review dispatched (sonnet).
  Task 4: dispatched (sonnet) at BASE 183ff8c.
  Task 2: review (sonnet): spec ✅, Approved, 0 findings. ⚠️ resolved by controller: delevel constants verified against `git -C ../UnderTheSea show ab1105e:scripts/globals.ash:150-187` (0.5/0.7/0.75, >0.25 floor, Null Afternoon gate) — exact match.
  Task 2: complete (commits 624cc54..84c3543, review clean)
  Task 1: fix round 1/5 re-review (sonnet): 3/3 ADDRESSED, no new breakage.
  Task 1: complete (commits f57b8b6..624cc54 + fix 183ff8c, review clean)
  Task 3: review (sonnet): spec ✅ (F10 odometer verified terminating + clue-preserving; chain order checked), Approved, 0 Important. ⚠️ resolved by controller: choices 396/397/398 (ChoiceControl.java:5084-5103, unlock prefs) and 705 (:7394) confirmed in mafia source.
  Task 3: minor (deferred): guard comment at choice.ts:262-264 says "4^6 is fine" while the F10 threshold routes 6 unknowns to the fallback — comment self-inconsistent; enumeration loop re-reads dreadScroll prefs instead of reusing `unknowns`.
  Task 3: complete (commits 84c3543..f6d3e25, review clean)
  Task 4: implemented 3a925a1 (DONE; engine.ts only); review dispatched (sonnet).
  Task 5: dispatched (opus) at BASE 3a925a1 carrying rulings F4, F5, F13, F17, F18.
  Task 4: review (sonnet): spec ✅, Approved, 0 findings (dreadSeedCheck call graph confirmed adventure/abort-free).
  Task 4: complete (commits 183ff8c..3a925a1, review clean)
  Task 5: implemented e586744 (DONE; concerns handed to reviewer: >400HP backstop outside stalled<14 cap; stuck>3 aborts on 4th repeat; Yog thrown dedup per item type). Review dispatched (opus).
  Tasks 6+7: dispatched as one unit (opus) at BASE e586744 carrying F4, F6, F11, F17, F19, F20, F21, F24.
  Tasks 6+7: implemented 0c64b14 (gym/colosseum) + f8800fc (skatepark/burn/fishy) (DONE_WITH_CONCERNS: retro cape real name `unwrapped knock-off retro superhero cape`; retrocape mode dance unexercised outside mafia; F24 narrow state — forcer pending + incomplete gladiator set → burn returns false). Review dispatched (opus).
  Task 8: dispatched (sonnet) at BASE f8800fc carrying F8, F9, F18.
  Task 5: review (opus): spec ❌ — 3 Important: (1) plan-mandated: gym branch unreachable (gladiators gate precedes it; Gymnasium roster is Mer-kin juicer/poseur/trainer per combats.txt:203, disjoint from the colosseum roster — the brief's "same monsters" premise is wrong); (2) JS filter `round` is mafia getRoundIndex() (0-based, minus preparatory rounds), not current_round() — nuke-lead window off by one and stale after prefix actions; (3) Club 'Em Back in Time branch unlatched → can burn 5/day in one fight or trip mafia's no-op abort.
  Task 5: Ruling on (1): the brief's premise is factually wrong (verified by reviewer against combats.txt:201/203); spec §5 requires the gym regime to run in the gym — in `opts.gym` mode the gym extras + the delevel/nuke ladder run for every Mer-kin-phylum monster in the gym (killscroll `phylum == mer-kin` guard CCS:1206 stays load-bearing), and the six-gladiator gate applies only in colosseum mode; while reworking the block, match CCS:1200-1210 ordering (scrolls first, then forcers) — cost if wrong: gym fights use the heavier ladder / one extra scroll.
  Task 5: Ruling on (2): use kolmafia `currentRound()` for the nuke-lead window and the stuck detector instead of the callback's `round` param (Macrofier.java:127 passes getRoundIndex()) — cost if wrong: a one-round shift in the opening.
  Task 5: minor (deferred): `stalled < 14` cap applied to fresh arms too (ash caps only renewals, CCS:481 unconditional); >400-HP backstop delta can span opener/scroll rounds (ash recaptures hpBefore after develOpeners, CCS:427-429) → possible spurious 10-round stall; `stuck` resets on every advancing round (ash accumulates per fight, CCS:340); shubFilter samples Null Afternoon at closure construction; stall countdown decrements before the wanderer bail.
  Task 5: fix round 1 dispatched (resume implementer).
  Task 5: fix round 1/5 (3 addressed per implementer, pending re-review; also extended currentRound() to yog/shub/center stuck detectors; commit eb4fd72); re-review dispatched (opus).
  Task 8: implemented 8041417 (DONE; F8 applied; F9 via free/Loded-budget loop in do() + freeDigAvailable()); review dispatched (opus).
  Task 9: dispatched (sonnet) at BASE 8041417.
  Task 9: implemented 917ac8f (DONE); review dispatched (sonnet).
  Task 10: dispatched (opus) at BASE 917ac8f carrying F18 (import order) and the F3 memo note.
  Task 5: fix round 1/5 re-review (opus): 3/3 ADDRESSED (+ currentRound() extension judged correct), no new Critical/Important.
  Task 5: minor (deferred): nuke-lead window `here <= 1` (fights.ts:183) is monster-independent — the ash gates on `freeRounds()` (1 for the colosseum six, 0 otherwise, CCS:165-174/417-419), so gym monsters now nuke on round 1 instead of receiving the openers; unlatched gym healscroll throws draw on the stock `yogHealOrder` also uses (ash-faithful; Task 12 reserve math should know); no free kill offered in the gym (ash gym case runs free_kill(page_text,false) before cleanUp) — belongs to gym.ts / free-kill task, not fights.ts.
  Task 5: complete (commits 3a925a1..e586744 + fix eb4fd72, review clean)
  Tasks 6+7: review (opus): ash regimen/Grandma rows/memo/lutz verified; Needs fixes — Critical (plan-mandated): colosseum ready uses itemAmount on gear mafia equips → only round 1 ever ready; Important: (1) burn.ts colosseum rung has no gear gate → adv1 refused, returns true, uncounted wait loops spin; (2) helpers re-maximize with no breathing rider and no familiar breather (Eagle/Foul Ball aren't aquatic; ash passes bathysphere(); skatepark -combat maximize lacks the trunks) — only survives under Driving Waterproofly; (3) gearQuest lacks !noncombatForcerActive guard → back-to-back gym passes hard-abort after banking; (4) retro cape equipped without `retrocape heck kill` mode → free fight silently lost.
  Tasks 6+7: Ruling (Critical): availableAmount for the gear checks — spec requires the 15-round grind — cost: none. Ruling (2): helpers must supply breathing themselves — reuse the engine's existing breathing/familiar-breather helpers (export minimally if needed) rather than a second rule set; the deepcity outfit zones supply PLAYER breathing via the Mer-kin outfit but never FAMILIAR breathing — cost if wrong: refused adventures on non-Asdon accounts. Ruling (3): add the guard; RUNPLAN CONSTRAINT for Task 14: gearQuest (gym) precedes skateParkQuest so the gym banks the in-combat forcer and the park spends it (ash UTS:645-647) — recorded here for the Task 14 dispatch. Ruling (4): set the mode before maximizing, as the parka dance already does.
  Tasks 6+7: minor (deferred): F21 comment sits above `ready` but describes `completed`; claimIceBuff reads stale skateParkStatus after the resolving choice (one Fishy turn lost); skate-park map — controller to verify an earlier phase acquires it (else skateWarOpen() is permanently true on a map-less account and burn rung 1 spins); burn.ts drops the ash's path/boss gate on gear/colosseum rungs (fine while Shub-only).
  Tasks 6+7: fix round 1 dispatched (resume implementer).
  Tasks 6+7: controller check — no phase buys the skate-park map; sim.ts:159 lists `mapToTheSkateParkPurchased` as an account checklist row (one-time purchase, persists). minor (deferred, for the final fix wave): skateWarOpen()/skateParkQuest/burn rung 1 should gate on `get("mapToTheSkateParkPurchased")` so a map-less account cannot spin on a permanently-"war" pref.
  Task 9: review (sonnet): spec ✅, Approved, 0 findings (MomRequest/SourceTerminal/PYEC prefs verified).
  Task 9: complete (commits 8041417..917ac8f, review clean)
  Task 8: review (opus): mining index math/shaft policy/F8/F9 verified vs MineDecorator.java + ash; Needs fixes — Important: (1) plan-mandated: retrieveItem on the ROW124/125 barter (and the 11-leaf clover) is a silent no-op with mafia's default `autoSatisfyWithCoinmasters=false` (InventoryManager.java:1568-1570, defaults.txt:241) — nothing in the repo sets it; (2) retrieveItem returns discarded; the abort blames fish scales for every failure.
  Task 8: Ruling (1): scope the pref locally — wrap the coinmaster retrievals in libram `withProperty("autoSatisfyWithCoinmasters", true, …)` inside mine.ts (no shared-file edit; auto-restored) — cost if wrong: none beyond a pref toggle during the call. Ruling (2): check each retrieveItem return and abort naming the missing input; and (from minor #3) `completed` for the tailpiece task gates on the real tailpieces (crappy/gladiator/scholar) not the teflon swim fins (an accessory) — a fins-only state must not mark the disguise done.
  Task 8: minor (deferred): soft limit counts do() attempts not inner digs (loop bounded by mafia parse + Loded + square flip); ash's pre-barter `unequip sea chaps; unequip aerated diving helmet` dropped (InventoryManager probably covers it); black glass not pinned to acc3; mineSquare() equips trunks unconditionally (ash equipSwimTrunks skips under Waterproofly); Crappy Mask `ready` redundant with `completed`. Note: other coinmaster retrievals elsewhere in src may share defect (1) — final review to grep `retrieveItem` for coinmaster-sourced items.
  Task 8: fix round 1 dispatched (resume implementer).
  Task 10: implemented 2c3c358 (DONE; school.ts only; residue: both cowl+rope on the last -combat trip leaves hallpasses closeted — handed to reviewer); review dispatched (opus).
  Tasks 6+7: fix round 1/5 (5 addressed per implementer; commit 3dbfe21; open question: gym/skate park familiar breathing when no outfit field) — re-review dispatched (opus).
  Task 11: dispatched (opus) at BASE 3dbfe21 carrying F12, F22, F23, F18.
  Tasks 6+7: fix round 1/5 re-review (opus): 4 addressed, 1 open — familiar breather missing in gymnasiumTurn/skateParkTurn (engine enforcement skipped for outfit-less tasks, engine.ts:240/262-266). Ruling: same pattern as colosseum at all three sites; abort loudly when no breather is owned (matches engine.ts:245). Fix round 2 dispatched.
  Tasks 6+7: minor (deferred): a forcer pending with the war closed leaves both Guard Grind and War Resolution unready (nothing consumes it) — note for Task 14 runplan; burn.ts colosseum rung tests only lastColosseumRoundWon<15 (benign: champion write sets 15).
  Task 11: implemented 7b70b44 (DONE; concern: scholarPieces equipped unguarded — reachable before scholar gear? handed to reviewer); review dispatched (opus).
  Task 12: dispatched (opus) at BASE 7b70b44 in AMENDED form carrying F13, F14, F23, F18.
  Task 8: fix round 1/5 (2 addressed per implementer + tailpieceOwned; commit 36d18e1); re-review dispatched (sonnet).
  Tasks 6+7: fix round 2/5 (1 addressed per implementer; commit 7a70e6d adds outfit.ts requiredFamiliarBreather()); re-review dispatched (sonnet). WATCH: Task 12 concurrently edits outfit.ts (expFamiliar) — verify 7a70e6d export survives Task 12 commit.
  Tasks 6+7: fix round 2/5 re-review (sonnet): ADDRESSED, no new breakage.
  Tasks 6+7: complete (commits e586744..f8800fc + fixes 3dbfe21, 7a70e6d, review clean)
  Task 10: review (opus): Needs fixes — Critical (plan-mandated): `pullsRemaining() > 0` as the cheatsheet-obtainable proxy deadlocks the long route (pullSequence refuses a same-day repeat; wordquiz consumes the cheatsheet) → Use Wordquiz no-ops until tries:15 aborts with the wrong cause; Important: (2) cheatsheet pullSequence has no pullBudgetAllows gate (plan-mandated); (3) School Unlocks' `|| cowlAndRope()` escape applies to the long route too (ash: short branch only, UTS:2596-2597) → unlock prefs never set, teacher/bunwig unreachable; (4) no isMerkinHighPriest guard → school re-opens after the gladiator sell-back flips cowlAndRope() false.
  Task 10: Rulings: (1)+(2) replace both proxies with `!pulledToday(cheatsheet) && pullBudgetAllows(cheatsheet)` (ash fallback: farm when the pull is gone); (3) `completed: teacherUnlock || (isKnucklebonesAndSushiEnough() && cowlAndRope())`; (4) quest-level `completed: () => get("isMerkinHighPriest")` (ash UTS:2501 wraps the block); minor #5 ruled IN (same file, one line, wasted turns): uneffect the sneak effects / re-mood before the +item farm tasks per UTS:2605 — cost if wrong: one uneffect.
  Task 10: minor (deferred): Farm School always reclaims hallpasses where the ash's getCheatsheet closets them (deviation undocumented in-comment); Duplicate comment wording.
  Task 10: fix round 1 dispatched (resume implementer).
  Task 8: fix round 1/5 re-review (sonnet): 2/2 ADDRESSED (+tailpieceOwned), no new breakage. minor (deferred): unreachable missing-input branches in the barter diagnostics (outer gates already guarantee them).
  Task 8: complete (commits eb4fd72..8041417 + fix 36d18e1, review clean)
  Task 11: review (opus): F12/F22/F23 faithful, High Priest loop non-spinning; 3 Important (all plan-mandated): (1) default throw macro compiles before the researcher forceItems action → killscroll kills the researcher before Use the Force (ash researcherForce at CCS:464 precedes throws); (2) forceGranted("researcher") has no "both scrolls held" guard (ash G:1019-1034 bails) → free Force wasted; (3) Library Farm has no ready — Library is outfit-gated (outfits.txt:65/adventures.txt:304); engine.ts:303 filters unowned equips so mafia refuses the zone and 30 no-op passes abort with a misleading message.
  Task 11: Rulings: (1) exclude the researcher from the clue-throw default macro while a researcher Force is wanted; (2) request forceItems(researcher) only while killscroll==0 || healscroll==0, else kill — keeps saber.ts untouched; (3) `ready: () => scholarPieces.every(have)` on Library Farm; ruled in: correct the wrong "grimoire throws" comment at library.ts:99-101 (engine filters unowned equips) — cost if wrong: none.
  Task 11: minor (deferred): monodent dropped from the -combat branch (ash keeps it in both); scrollsMissing wider than the ash's saber predicate (F12 pinned it); avoid:[BCZ] stronger than the ash's omission; knucklebone/worktea pulls behind pullBudgetAllows where the ash pulls unconditionally (F23) — consider promoting them to pullReservations at the final review.
  Task 11: fix round 1 dispatched (resume implementer).
  Task 10: fix round 1/5 (5 addressed per implementer; commit 30d893d); re-review dispatched (sonnet).
  Task 10: fix round 1/5 re-review (sonnet): 5/5 ADDRESSED, no new breakage.
  Task 10: complete (commits 917ac8f..2c3c358 + fix 30d893d, review clean)
  Task 11: fix round 1/5 (3 addressed per implementer; commit d74e728; farm split into Force/plain lanes — two soft:30 counters flagged); re-review dispatched (opus).
  Task 12: committed ca3e3cd (yogurt.ts + fights.ts yogUrtFilter amendment + outfit.ts expFamiliar; both outfit.ts helpers verified present) — awaiting implementer report before review dispatch.
  Task 12: implemented ca3e3cd (COMPLETE; 3 deliberate divergences — maxHeal throw-order walk, budget-driven crystal/band-aid top-up, Gummiheart burn flag-not-abort — handed to reviewer); review dispatched (opus).
  Task 13: dispatched (opus) at BASE d74e728 carrying F7, F14, F23, F18.
  Task 11: fix round 1/5 re-review (opus): 3/3 ADDRESSED; two-lane split judged necessary (grimoire `combat` is not Delayed).
  Task 11: minor (deferred, final fix wave): library.ts:128 `saberForResearcher` should be `researcherForceWanted() && saberForcesFree() > 0 && have(saber)` (ash UTS:770-772) — today the 4-way scrollsMissing suppresses the monodent in the plain lane with no saber to replace it, and excludes researcher throws on saberless accounts; two lane names → 60 library attempts vs the plan's 30 (consider ~15 each or a shared counter); both lanes un-ready forever without scholar gear (silent no-op — consider limit.unready or a print); bczWanted() called twice.
  Task 11: RUNPLAN CONSTRAINT for Task 14: runplans.ts must keep calling getTasks() WITHOUT implicitAfter — the two Library lanes share `completed`, so an implicit `after` chain would deadlock the plain lane.
  Task 11: complete (commits 3dbfe21..7b70b44 + fix d74e728, review clean)
  Task 13: implemented 39beed0 (COMPLETE; deviations: insurance pulls budget-gated, Medal have()-guarded, pearlsPried() reused as Seaceress ready); review dispatched (opus).
  Task 14: dispatched (opus) at BASE 39beed0 carrying F15 (parasol no-port), F16 (README sim line), gym-before-skatepark ordering, no implicitAfter, forcer-pending note.
  Task 12: review (opus): spec ✅ (§1-§4 term-by-term vs 89982f5; all three divergences judged correct — maxHeal throw-order walk is the amendment's normative meaning; budget-driven top-up strictly stricter than the ash; ladder-dry flag bounded at 8 stalls + soft:40), Approved, 0 Important. ⚠ resolved: yogHealingsShort() only colors the abort — the farm loop it drives upstream lives in outpost.ts (availableAmount(beads) >= 3), out of Task 12's scope.
  Task 12: minor (deferred): availableAmount vs itemAmount asymmetry (closeted heals count but can't be thrown — upstream-identical); port aborts where the ash falls back to farmPrayerbeads (outpost task covers earlier); burn task ready includes myAdventures() > 0 (fight never ready at 0 adv — harmless); yogHealingsShort() reads as unused helper — comment it.
  Task 12: complete (commits 30d893d..ca3e3cd, review clean)
  Task 14: implemented db6258f (COMPLETE; skateParkQuest moved after gearQuest per ruling; F15 no-port + F16 applied; no cruft found); review dispatched (opus).
  Task 13: review (opus): faithful (delevel math, prep ordering, door gating, penny dump — sea gel NOT in the ash's dump, UTS:2985-2995 — implementer right); Needs fixes — Important: (1) plan-mandated: collapsed null-day pull/use chain never uses an exploit already in inventory and can pull/buy a duplicate (ash UTS:2895-2897 keeps two statements); (2) expFamiliar() returns undefined on accounts with none of the three → outfit familiar no-op → last-fought (attacking) familiar rides into Shub, the exact hole the amendment closes.
  Task 13: Rulings: (1) restore the ash shape — pull only when itemAmount==0, then `if (itemAmount>0) use`; (2) fix at the shared helper: `expFamiliar(): Familiar` returning `$familiar.none` as the terminal rung (amendment text; sibling meatFamiliar does the same) — signature change from the F14 contract, yogurt.ts call site needs no edit; (3) ⚠️ ruled in: Center Door `ready` also requires 5 unblemished pearls in inventory after the pry, abort with instructions otherwise (spec §9 init guard only covers turn 0) — cost if wrong: one extra predicate.
  Task 13: minor (deferred): finale.ts:78-80 comment claims the postloop hook fires when every task completes, but prep tasks (Shub Prep, Yog Prep) go incomplete-but-unavailable after their fight so main.ts `remaining.length === 0` never holds — REPO-LEVEL pattern for the final review (prep tasks should count complete once their fight is won); Pry Pearls / Penny Dump limits lack messages; recover() 250-MP default thin for the Seaceress; inline codpiece scan vs EternityCodpiece.currentGems(). Out of scope note: pulls.ts:85 flattens the ash's conditional `yogUrtDefeated ? 0 : 2` to a constant 2 and drops the `item_amount(null-day exploit)==0` clause.
  Task 13: fix round 1 dispatched (resume implementer).
  Task 13: fix round 1/5 (3 addressed per implementer; commit 06d5744; expFamiliar signature now Familiar); re-review dispatched (sonnet).
  Task 14: review (opus): spec ✅ (ordering/supply chain verified task-by-task; no implicitAfter; F15/F16; cruft absent; Phase 3 intact), Approved, 0 Important.
  Task 14: minor (deferred): README:14-21 "sim checks this and everything below" overclaims — the photobooth/Sheriff-kit bullet is only checked at init.ts:131-135 (add a sim row or move the bullet); gear-before-park defers the free Lutz Fishy in the no-wait branch (consumables, not turns) — note in runplans comment.
  Task 14: complete (commits 39beed0..db6258f, review clean)
  Controller verification at HEAD 06d5744: yarn check clean, yarn lint clean, yarn build three bundles (pre-existing mafia-shared-relay eval warning only).
  Task 13: fix round 1/5 re-review (sonnet): 2/3 addressed; finding 2 partial — $familiar.none terminal activates unguarded engine.ts:240 familiar-breather block → Failed to fully dress on accounts with no exp familiar and no breathing effect. Ruling: one-line engine guard `outfit.familiar !== $familiar.none` (mirrors outfit.ts:85); fix round 2 dispatched.
  Task 13: fix round 2/5 (engine.ts:240 none-guard + finale comment; commit cf61eaf); re-review dispatched (sonnet).
  Task 13: fix round 2/5 re-review (sonnet): ADDRESSED, no new breakage.
  Task 13: complete (commits d74e728..39beed0 + fixes 06d5744, cf61eaf, review clean)

## Final whole-branch review

Dispatched (fable) on review-9aa261b..cf61eaf.diff (25 commits) with the ledger deferred-minor/ruling lines as triage input.
USER DIRECTIVE (2026-08-27): remove the "Mom Buff" task (src/tasks/sorceress/daily.ts, plan Task 9 addition — not an ash port) after the final review lands; fold into the final fix wave together with a one-line plan-doc amendment (deferral table: Mom daily buff IN → DROP, user decision).
Final review (fable): NOT ready — Critical 1: library lanes re-open after High Priesthood (dreadscroll consumed on success → farmCompleted false; ~30 turns + wrong-cause abort every run); Important 2: no mapToTheSkateParkPurchased gate; 3: knucklebone/worktea pulls budget-gated (ash unconditional) — abort with pulls remaining; 4: prep tasks incomplete-but-unavailable → postloopCommand unreachable (plan defect: exit criterion unsatisfiable); 5: mine.ts abyss trips lack familiar breather; 6: silent stop with pending forcer + closed war. Minors 7-14. Triage: FIX-BEFORE-MERGE = I2, I3, I4, I6 (+C1); rest FOLLOW-UP/DROP per the review. Rulings check: all stand; plan exit criterion to be reworded.
Final fix wave Rulings: I4 — adopt route-terminal `routeComplete()` (finale.ts) as the postloop trigger + per-task completed ORs + one-line plan exit-criterion amendment — cost if wrong: postloop timing; I6 — option A (Guard Grind ready when forcer pending only if war closed → gymnasiumTurn abort-with-instructions) — cost if wrong: one abort instead of a silent stop; I3 — promote knucklebone/worktea to pullReservations (>= branch) — cost if wrong: one reserved slot each; I5 also takes Minor 7 (pre-barter unequips) while in mine.ts; U1 Mom Buff removal + plan table row IN→DROP per user; M13 README one-liner.
Final fix wave dispatched (opus) with brief final-fix-wave-brief.md at BASE cf61eaf.
Final fix wave: implemented cf61eaf..582edad (7 commits; C1, I2, I3, I4a+b+plan line, I5+m7, I6, U1+plan row, M13; widenings: worktea reservation adds mastery<90, PYEC completed adds !usePyec); scoped re-review dispatched (opus).
Controller verification at HEAD 582edad: yarn check clean, yarn lint clean, yarn build 3 bundles; tree clean (docs/superpowers/research/ untracked only); 32 commits over main; Mom Buff absent from daily.ts and README.
Final fix wave re-review (opus): ALL ADDRESSED, no new breakage. Residual out-of-scope: plan doc Task 9 snippet still carries the Mom Buff task and a stale postloop comment (docs only — FOLLOW-UP); mom.ts Banish Constructs completed can flip back (stale list mark; harmless); the 2 new reservations narrow discretionaryPull in the dreadscroll→clues window (intended); README photobooth "aborts with instructions" is only true in the wrong-kit branch (init.ts limit lacks a message — FOLLOW-UP). Ruling: park all four as follow-ups; branch is merge-ready at 582edad.
PHASE 4 COMPLETE — 32 commits, final review + fix wave clean. Proceeding to finishing-a-development-branch.

## Post-review user feedback rounds (2026-08-27, after the SDD ledger closed)

- 2067785 mining aborts after the free period (reviewed clean).
- 46d785d/27fcb12/b2c4998 sea keyword (gated on !hasBreathingEffect), YogHpCheck 0.8x + [3]=1, mineSquare trunks removal; fix round 43d687b/bffd29e (-tie for bare outfits; sea gated on real familiar w/ hard-equip fallback; helpers check maximize() + ensureHelperBreathing(); Yog abort scoped <3 beads) — re-review clean; comment premise about $familiar.none corrected in the next wave.
- Grimoire-bypass audit: docs/superpowers/research/2026-08-27-grimoire-bypass-audit.md (3 HARMFUL, 11 REDUNDANT, 20 NECESSARY, 4 engine gaps); user approved fix wave items 1-10 + documentation comments.
- Bypass fix wave bffd29e..0fd2e46 (8 commits) reviewed: all items addressed/justified; 1 Important regression (ensureHelperBreathing after the Grandma hat blanking can re-wear the crappy-mask token) → ruling: no breathing pass at the two trade sites (GrandmaRequest has no breathing gate); minors: have()-gate the lasso +equip terms; bead count = itemAmount+equippedAmount (closet excluded). Follow-ups recorded by the reviewer: engine.ts:299 famequip enforcement should read `outfit.familiar ?? myFamiliar()` (would retire the mine.ts riders); adv1 filter closures bypass the engine starting macro (lasso throw dead in dynamic fights); a `strip` outfit field would close the coinmaster-token gap declaratively; colosseum ensureHelperBreathing vs retro-cape back slot.
- Round 2 bf72349: Grandma trade breathing calls removed; lasso terms have()-gated; bead count itemAmount+equippedAmount. Controller verification at bf72349: check/lint clean, 3 bundles.
- Live smoke test 2026-08-27: Init/Toot failed (questM05Toot stays "started" on the Sea path — TutorialRequest only flips on the Toot page text; the letter comes from council). Fix 524aae3: questlog.php?which=1 refresh + tries:2. Tier auto-detect showed mid because garbo_valueOfFreeFight is unset (0 > 6000 false) — ash rule; override via tier=high or set the pref.
- Toot round 2 (53bc8fe): the Sea path never closes the Toot quest (quest log keeps listing it), so completion = daily `_subaqua_toot_visited` marker (Pearl Guard pattern); questlog refresh kept as a no-op safety net.
- Toot round 3 (a2397f6): the Sea-path council/Toot pages open a one-option "Right, okay" dialog; do() drains it after each visit (handlingChoice → runChoice(1)).
- Sword Imprint lost at level 1 (kill vs a sea cowboy; imprint itself landed at round 2). Ash SWordLasso free-kills it (freeKill() gear + CCS free_kill) — fix in flight: .killFree(sea cowboy) drop-safe + ready gated on an available source.
- Sword Imprint fix 0ccf0d7: .killFree(sea cowboy) after the %fn opener; ready gated on selectFreeKill({dropsMatter:true}). Follow-up: engine.ts:172-179 calls selectFreeKill without dropsMatter — drop safety currently holds only because non-drop-safe sources sort last; thread a task-level dropsMatter through the engine.
- Combat port b1ab93e/edb5703/854b18b: full ash moods (itdrop/superitdrop/combat/-combat/res + song trim), delevel openers in killMacro (unconditional — BALLS has no monsterattack predicate), opportunistic free kills at the ash free_kill sites (17-site table in the report; wild seahorse excluded). Rulings: squint moves to prepare (post-dress); conserveFreeFights no longer disables the upgrade (dartsOnly is the limiter, ash parity).
- Combat port review (opus): Critical — free-kill step prepended ahead of task general macros (breaks Banish Constructs screech, library clue throws, corral sword opener) → ruling: drop prepend; Important: Best Pals daily gate; Bloodbathed bczCost gate; squint must not fire on Force fights (ash UTS:1395-1402); trimSongs keep-last (ash evicts oldest, +item songs survive). Minors: Micrometeorite per-day potency decay cost (ash-parity), Weaksauce mpabove gate, gym comment cite. Fix round dispatched.
- Combat port fix 720b6f5: prepend dropped (verified vs grimoire compile order), Best Pals/Bloodbathed gates, squint Force gate, trimSongs keep-last. Re-review caught a regression: `mpabove` is not a BALLS predicate (macrohelper.6.js:101-134) → round 2: `ifNot(mpbelow cost)`; stale prepend comments; gym abort cite.
- Round 2 1e9248b re-reviewed clean (BALLS emission traced through libram to `if !mpbelow <cost>`; predicate audit holds). Deployed 1e9248b. Wave 2 (the garbo fork 2-5) dispatched.
- Wave 2 (e3c67e7..18dcdc4) review: items 2-4 verified; Critical on item 5 — Monodent bolt is NOT turn-free (BanishManager.java:118) while every displaced source is; peridot-forcing the rustler spends the zone peril → ruling: revert Monodent-first (fallback only), peridot → the drop monster (cow/cowboy). Important: killMacro openers get openerOnce; Shub item-cure filtered by moodWouldSpend; MP top-up before pre-fight moods + Elron item-cast guard; minors: Lyle gate, mafia shruggable whitelist, comments. Fix round dispatched.
- Wave 2 fix 03f6af9 re-review: Critical/Imp 3/Imp 4/minors ADDRESSED (peridot → drop monsters, ash banish order, moodWouldSpend filter, MP reserve via acquireEffects override, Lyle fail-silent, mafia shruggable ids). OPEN: openerOnce guard round in killMacro can not see per-monster task macros → openers skipped on Corral Leather/Lassos/Golem Recall — ruling: add slack (leadingActions + 3); minor: reserveMpFor should return early for effect-less tasks; pre-existing: Spring Kick listed twice in banishSources (ungated dup). Follow-up: sort the banish ladder by turn-freeness (System Sweep etc. non-turn-free above turn-free sources) — separate change. Queued behind wave 3.
- Wave 3 (115e907..b1ae313): daily sea jelly harvest; banish-hold invariant (abort with instructions, bounded incl. a turn-passed check); 25% HP floor in stall/runaway loops (monsterAttack is the to-hit stat, not damage); thrown-item audit — no fixes (mafia has no underwater-usability flag; ash production use cited). Wave-2 round 2 (opener slack, MP early return, Spring Kick dup) dispatched on top.
- Wave 3 + guard fix review (opus): items 6/7/9 + wave-2 round 2 approved; Critical — HP floor skill-first heal (20 HP) preempts sea gel and loops in the Yog tail; Important — corral runaway macro drains Yog sea-gel reserve (build-time check only); Sea Jelly can abort when choiceAdventure1219 auto-resolves. Rulings: items-only floor heal ordered by size and only when it clears the floor; non-reserved unguent in the runaway loop; choices {1219:1} + daily marker. Minors folded (Time-Spinner comment, outpost dup list, banish recency bound, spleen guard). Fix round dispatched.
- Wave 3 fix c84be8a re-reviewed clean (items-only floorClearingHeal keyed to ash HealingHP; unguent-only runaway heal capped !pastround 6; Sea Jelly choices+marker; banish recency per-task; spleen guard). Nits (follow-up): floorClearingHeal takes the biggest spare heal rather than smallest-sufficient; stallAction can now spend a duplicate Mer-kin healscroll the library wants two of (narrow). Deployed c84be8a.
- User rules 2026-08-27: healing skills limited to Cocoon + Walrus (engine hpAutoRecoveryItems skill filter); Yog-Urt casts nothing under More Like a Suckrament (filter throws kit items/attacks). Commit 5a4f3b7 deployed on user request before review; review running.
- Live: Init/Sea Jelly aborted on stale Beaten Up + \_lastCombatLost from the earlier imprint loss (throw preceded the cure) → fix in flight: cure first, task-scoped loss check. Live: Ensure Error on Richie Thingfinder — 3 songs already active; trimSongs never shrugs an unwanted active song → fix in flight: shrug unwanted active AT songs before casting at the cap.
- Engine fixes 1dfb834 (cure Beaten Up first; task-scoped loss check via pre-task snapshot) + af13492 (shrugForSongs in applyEffects; currentMood=apathetic managed for the session — user mood lose_effect triggers re-cast shrugged songs). Deployed af13492; review running.
- Engine fixes review: mood/song ADDRESSED; Important — loss-check encounter clause fires on NCs too (AdventureRequest.java:597) → ruling: count only monster encounters (toMonster(lastEncounter) !== none). Song-trim choke point ed5263c deployed; the live Richie failure is NOT the untrimmed-list theory (lists were already trimmed) — maxSongs()=4 via Mariachi Memory suspected; awaiting the gCLI red text from the user.
- ef33099: loss check counts only monster encounters (controller-verified diff; deployed). Branch HEAD ef33099.
- Live Richie failure root cause: The Ballad of Richie Thingfinder costs 50 MP (classskills.txt:555) > early-run max MP; mafia refuses the cast, libram ensureEffect throws. Fix in flight: engine acquireEffects fail-soft (reproduces grimoire loop; yellow note on failure) + skip skills with mpCost > myMaxmp; same in applyEffects.
- ed7f72a: acquireEffects/applyEffects fail-soft (grimoire loop reproduced; yellow line per failure), skip skills with mpCost > myMaxmp, per-task "Effects for <task>: …" print + skip lines (user request). Deployed; review running.
- Live: Garden Pellet vs diving belle — compiled general action was the freeRun taffy degrade (no taffy → 0 actions → KoL macro abort) despite task default .banish(); free-kill monsters had full ladders. Fix in flight: root cause + invariant (freeRun underwater = taffy-then-kill; provided banish/freeRun macros get the kill ladder as fallback).
- Fail-soft review: Approved; watch-item — catch could swallow a mafia abort() (JS exception in mafia runtime) → add-on: catch only EnsureError, rethrow others; label applyEffects contexts.
- Empty-macro root cause: Grandpa/Find Grandpa (.kill(trench trio).freeRun()) fields the Peace Turkey, so the Stomping Boots free-run provide was equip-dropped and the underwater freeRun degrade was taffy-only → 0 actions. Fix 17665e7: every compiled action ends in the kill ladder (freeRun = taffy-then-kill; provides retry the ladder with an exclude set); dd919ff: catch only EnsureError, labeled helper effect lines. Deployed dd919ff; review running. Open ruling: the fallback kill can burn the once-daily darts bullseye (ELR) on a run-source-less fight.
- Empty-macro fix review: Approved. Follow-ups dispatched: delayed fallback (post-dress), bullseye-suppressed fallback kill ladder, run→free-kill fallthrough excludes once-daily sources. Open design call for the user: Peace Turkey (sneak familiar) on every freeRun task makes the Stomping Boots (24 runaways/day) unequippable.
- USER DECISION: free-run familiar rule = boots always in +combat contexts; sneak familiar first and boots last-resort in -combat contexts (queued after the fallback follow-up).
- 9f932b7/704d71a: delayed post-dress fallback ladders, bullseye-suppressed fallbacks, run→free-kill fallthrough excludes once-daily sources. Deployed 704d71a. Boots-rule implementation dispatched.
- USER CORRECTION: darts bullseye replenishes (ELR cooldown), not once-daily → revert its exclusion from run fallthrough and fallback ladders (folded into the boots-rule commit).
- 62cedbd: free-run familiar rule (boots in +combat contexts; sneak first, boots last resort in -combat; explicit familiars never overridden) + darts correction (bullseye allowed in fallbacks/fallthrough; only long-cooldown sources excluded). Deployed; review running.
- Live @ Outpost Grandma: Richie ensure failed again (fail-soft OK; cause unknown — mafia marks Ocelot attribute none, so song count matches mafia; awaiting `cast` output from user); choice 1599 (Legendary Digestion) unsupported → user: always 1; "2-3" toInt warnings; Fishy ladder mall-bought+pulled Frutti di Scatoletta (flagged to user). Fix dispatched for 1599/range parse/diagnostic failure line.
- Live: Mom/Black Glass bought at step9 — KoL refuses (sale opens at step11 per questslog.txt:72; steps 9/10 need the Little Brother / Big Brother visits the ash does first). Fix in flight: step walker 9→11 then buy.
- 94d3e2d/5b6c92f/17fac63: choice 1599→1 (Legendary Digestion), averageAdventures() range parse, diagnostic effect-failure lines + current-MP pre-check, pure sneak probe + boots gate, Black Glass step walker 9→11. Deployed 17fac63; review running.
- Live @ Abyss Mom: Richie diag "songs 3/4" → real cap 3: mirror mafia songLimit() (Four Songs modifier + Additional Song) instead of have(Mariachi Memory). "On your own" on macro submit: compiled macro has an EMPTY per-monster block and a parenthesized negation `if !(monsterid 778)`; fix in flight (no empty blocks; positive per-monster emission).
- Review of 94d3e2d/5b6c92f/17fac63: Approved (1599 cite, averageAdventures walk, MP pre-check parity, Black Glass transitions verified vs QuestManager.java:1436-1447); minors: mom.ts step-walker else-branch lacks a diagnostic; boots gate change is a no-op today.
- Live @ Mine Teflon (Phase 4 boundary): ~30 mining.php requests with no dig parsed; \_unaccompaniedMinerUsed=4 so freeDigAvailable stayed true → unbounded loop until user Stop. Fix in flight: bounded, progress-checked loop + diagnostic abort + digpick/Beaten Up prechecks + square-index verification.
- ee5d2d8 (no bodyless if…endif blocks) + 84a3fda (bounded mining loop, diagnostic abort; square math verified vs MineDecorator) deployed. Song-cap/negated-group half of the macro fix still in flight.
- a807f83: maxSongs mirrors mafia songLimit (Four Songs/Additional Song modifiers); hobo AT songs (Richie) requested only for Accordion Thieves — root cause of every Richie failure: Pastamancer cannot cast a Hobopolis AT song (UseSkillRequest.getMaximumCast returns 0). ee5d2d8: bodyless block root cause = libram empty Macro.toString() is ";" (length 1) so grimoire’s empty-macro guard misses it (VHS macro on Abyss targets). Both already in the deployed 84a3fda; review running.
- Review of a807f83/ee5d2d8: Approved with follow-ups — monsterMacro emits a positive parenthesized group (novel shape; revert to unparenthesized string guard); corral sword opener moved to the default slot now compiles after the engine free-kill step (re-register as a monster macro). [52] abort still open. Follow-up dispatched.
- 3b192ac: monster guards emitted as grimoire-identical unparenthesized strings; corral sword opener re-registered as a cowboy monster macro (lassoCombat()); stale Richie comment fixed. Deployed.

## Live-test follow-ups (2026-08-27, late)

- `src/resources/freerun.ts:322` and `src/resources/banish.ts:195` call `appearanceRates(location)` without `includeQueue: true`; only the stateful call runs `AreaCombatData.recalculate()` → `adjustConditionalWeighting()` (hatch rule etc.), so they can read a stale snapshot. `src/resources/peridot.ts` uses the stateful form; align the other two.
- `getLucky()` (mine.ts) has no 11-leaf clover pull rung; user decision pending (pull-budget slack gate).
- Minin' dynamite auto-pull after Loded: user decision pending (default manual).
